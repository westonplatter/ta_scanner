name: myenv

channels:
    - default

dependencies:
    - python=3.8
    - click
    - sqlalchemy
    - pandas >= 1.0.0
    - psycopg2
    - numpy
    - requests
    - pytz
    # - matplotlib
    # test resources
    # - pytest
    - pip
    - pip:
        # - twine
        - ipdb
        - loguru
        - ib_insync
        # - trading_calendars
        # testing resources